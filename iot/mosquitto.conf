# ---- Listener principal (TCP sin TLS, solo DEV) ----
listener 1883
protocol mqtt

# ---- Seguridad (DEV) ----
# En desarrollo permitimos conexiones sin usuario/clave.
# En producción: allow_anonymous false + password_file (lo haremos más adelante).
allow_anonymous true

# ---- Persistencia de sesión/mensajes ----
persistence true
persistence_location /mosquitto/data/

# ---- Logging a la salida estándar (visible en docker logs) ----
log_dest stdout
log_type error
log_type warning
log_type notice
log_type information

# ---- Ajustes opcionales ----
# autosave_interval 1800
# max_inflight_messages 20
# max_queued_messages 1000
